interface:
  display_name: "Simplify"
  short_description: "Review changed code and fix quality issues"
  default_prompt: "Use $simplify to review the changed code and fix justified quality issues."

policy:
  allow_implicit_invocation: false
