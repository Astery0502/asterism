{
  "schema_version": 1,
  "contract_id": "scientific-html-plan-v1",
  "plan_schema_version": 1,
  "profile_field": "toolchain_profile_id",
  "required_fields": [
    "schema_version",
    "plan_id",
    "target",
    "toolchain_profile_id",
    "representation_intent_id",
    "requirements_id",
    "dependencies",
    "interface",
    "requirement_capability_coverage",
    "computation",
    "numeric_contract",
    "views",
    "interactions",
    "delivery",
    "evidence_applicability",
    "artifact_inventory",
    "verification",
    "unmet_requirements"
  ],
  "required_nonempty_fields": [
    "plan_id",
    "dependencies",
    "interface",
    "requirement_capability_coverage",
    "computation",
    "numeric_contract",
    "views",
    "delivery",
    "artifact_inventory",
    "verification"
  ],
  "object_fields": {
    "interface": ["controls", "anchors", "views", "exports"],
    "computation": ["timing", "method", "capabilities"],
    "numeric_contract": ["domains", "tolerances", "sampling", "invariants", "boundary_or_initial_conditions"],
    "delivery": ["topology", "connectivity"],
    "verification": ["runner", "numerical", "browser"]
  },
  "record_list_fields": {
    "dependencies": ["name", "version", "license", "source"],
    "requirement_capability_coverage": ["requirement_id", "capability_domain", "capability", "status"],
    "views": ["id", "kind", "encoding", "scale_policy", "state_binding"],
    "interactions": ["id", "kind", "update_policy", "invalid_state_policy"]
  },
  "string_list_fields": ["artifact_inventory", "unmet_requirements"],
  "allowed_coverage_statuses": ["covered", "conditional", "unmet"],
  "conditional_evidence_checks": [
    "invariants",
    "boundary-or-initial-conditions",
    "bounds",
    "steps",
    "defaults",
    "named-anchors",
    "linked-view-updates",
    "invalid-state-handling",
    "control-causes-expected-state-change",
    "offline-network-block-when-declared"
  ],
  "allowed_applicability_statuses": ["required", "not_applicable"],
  "evidence_applicability_fields": ["check_id", "status", "basis", "requirement_refs"],
  "verification_recipe_fields": ["executor", "argv", "script", "expected_record"],
  "capability_domains": {
    "computation": "computation",
    "view": "views",
    "interaction": "interaction",
    "delivery": "delivery_modes"
  }
}
